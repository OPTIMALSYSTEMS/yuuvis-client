import { TextOverflowTitleDirective } from './text-overflow-title.directive';

describe('TextOverflowTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new TextOverflowTitleDirective();
    expect(directive).toBeTruthy();
  });
});
